const prefix = 'vuex-cognito-auth';

export const AUTHENTICATE = `${prefix}/AUTHENTICATE`;
export const AUTHENTICATE_FAILURE = `${prefix}/AUTHENTICATE_FAILURE`;
export const SIGNUP = `${prefix}/SIGNUP`;
export const SIGNUP_FAILURE = `${prefix}/SIGNUP_FAILURE`;

export const CONFIRMED = `${prefix}/CONFIRMED`;
export const CONFIRMED_FAILURE = `${prefix}/CONFIRMED_FAILURE`;

export const PASSWORD_CONFIRMED = `${prefix}/PASSWORD_CONFIRMED`;
export const PASSWORD_CONFIRMED_FAILURE = `${prefix}/PASSWORD_CONFIRMED_FAILURE`;
